# CaseHub Lite Product Override
# Usage: docker compose -f docker-compose.yml -f docker-compose.lite.yml up

services:
  casehub:
    environment:
      - CASEHUB_PRODUCT=lite
      - DEFAULT_LOCALE=pt-BR
