# PR Triage — 2026-05-22 (D-3 alpha 25/05)

> Frente E do goal alpha. Classifica os PRs abertos por owner (Codex frontend / Claude backend), maturidade, e urgência alpha-blocker. Acionável: aplicar labels + autorizar merge wave.
>
> **Base:** `origin/main` `dc00e97` (post-merge wave 22:14 BRT: #553/#558/#560/#556).
> **Generated:** Claude Opus 4.7 — `session-2026-05-22-git-triage`.

---

## 1. Sumário executivo

- **43 PRs abertos**, todos ≤2 dias (zero stale >30d → nada pra `wontfix`).
- **5 PRs meus desta sessão** prontos pra merge (#571 audit / #572 portal / #573 docs-page / #574 drive / #575 maestro).
- **#567 precisa ser CLOSED** — commit revertido em main (`57a109f` revertou `29dadee`); re-mergear re-introduziria o bug original.
- **3 PRs CLEAN** já mergeable em main (sem rebase): #544 #545 #550 (whatsapp/integrations/pdpj pool).
- **20+ PRs BEHIND** após merge wave — precisam rebase ou merge admin.

---

## 2. Tabela de triage

### 2.1 PRs MEUS — session 2026-05-22 alpha-readiness (BLOCKED, ready)

| PR | Slot | Estado | Recomendação |
|---|---|---|---|
| **#571** | F — audit doc baseline + diagnostics | BLOCKED | **MERGE ADMIN** — doc-only, zero risk |
| **#572** | A — portal_access defensive (P0 5/6) | BLOCKED, 3 tests pass | **MERGE ADMIN** — cirúrgico, removes 500 |
| **#573** | A — docs-page shared templates (P0 6/6) | BLOCKED, 3 tests pass | **MERGE ADMIN** — fecha última família P0 |
| **#574** | C — Drive REST explorer | BLOCKED, 8 tests pass | **MERGE ADMIN** — alpha-critical |
| **#575** | D — Maestro learning backend | BLOCKED, 8 tests pass | **MERGE ADMIN** — alpha-pretty-critical (feature flag default OFF) |

### 2.2 CLEAN (não-stacked, mergeable diretamente)

| PR | Title | Owner | Recomendação |
|---|---|---|---|
| #544 | feat(integrations-gateway): outbox + retry semantics | Claude | **MERGE** se sentinela aprovou |
| #545 | perf(whatsapp): pooled httpx client (bridge) | Claude | **MERGE** (predecessor de #550) |
| #550 | perf(pdpj): pooled httpx client (Council-approved) | Claude | **MERGE** (Council-approved; #545 first) |

### 2.3 STACKED (não-direct-to-main, mas mergeable em chain)

| PR | Base | Recomendação |
|---|---|---|
| #545 | integration/whatsapp-clone-2026-05 (#513) | Rebase sobre main OU merge #513 first |
| #550 | session-2026-05-22-wa-bridge-pool (#545) | Sequencial após #545 |

### 2.4 Claude-backend BEHIND (precisa rebase ou admin-merge)

| PR | Title | Urgência | Recomendação |
|---|---|---|---|
| #547 | perf(whatsapp): composite index | alpha-perf | MERGE |
| #548 | perf(whatsapp): batch list_conversations (N+1) | alpha-perf | MERGE |
| #549 | chore(whatsapp-bot): archive 17 orphans | low | merge or defer |
| #551 | fix(whatsapp-lite): rollback session | alpha-stability | MERGE |
| #552 | fix(whatsapp): rollback dashboard | alpha-stability | MERGE |
| #555 | perf(messaging-hub): non-blocking | alpha-perf | MERGE |
| #557 | perf(leads): conversion feedback async | alpha-perf | MERGE |
| #561 | perf(calendar): batch case/client (N+1) | **alpha-blocker** (módulo Agenda) | **MERGE ADMIN** |
| #562 | perf(notifications): batch deadline | alpha-perf | MERGE |
| #563 | perf(documents): batch bulk_delete | alpha-stability | MERGE |
| #564 | fix(efiling): create_submission + N+1 | alpha-fix | MERGE (immigration only) |
| #565 | fix(documents-api): batch_approve_documents + N+1 | alpha-fix | MERGE |
| #566 | perf(onboarding): batch existence check | alpha-perf | MERGE |
| **#567** | perf(package-maker): N+1 | ⚠️ **REVERTED IN MAIN** | **CLOSE** (commit revertido em `57a109f`) |
| #525 | feat(integrations-gateway): rota status + credential store | low | merge or defer |

### 2.5 Codex-frontend BEHIND (UI polish)

| PR | Title | Alpha-relevance |
|---|---|---|
| #512 | feat(ui): polish Maestro and WhatsApp surfaces | low (Maestro off) |
| #513 | feat(whatsapp): clone web.whatsapp.com Tier 1+2+3 | **post-alpha** (BIG, new feature) |
| #515 | fix(routes): consolidate P0 render fallbacks | alpha-stability |
| #516 | fix(mobile): stack case/process lists | alpha-mobile |
| #517 | fix(a11y): release notice focus | a11y |
| #521 | fix(controladoria): cap initial render payload | **alpha-blocker** (módulo Controladoria) |
| #522 | fix(aila): wiki home timeout | low (AILA off em Lite) |
| #523 | fix(tasks): gate Notion form in lite | alpha-cleanup |
| #524 | fix(admin): prefix GDrive sync fetches | admin only |
| #526 | fix(calendar): mobile agenda layout | **alpha-mobile** (módulo Agenda) |
| #527 | fix(documents): upload form mobile | alpha-mobile |
| #528 | fix(finance): polish mobile finance grids | low (Finance not alpha) |
| #530 | fix(a11y): consolidate tap target polish | a11y |
| #543 | [codex] Add profile header and floating dock | DRAFT, post-alpha |
| #546 | fix(tasks): polish task calendar mobile | alpha-mobile |
| #554 | docs(uiux): refresh Claude Design polish regime | DRAFT, docs |
| #559 | fix(tasks): polish kanban header hierarchy | UI polish |
| #568 | fix(ui): harden 404 and Basic logo chrome | alpha-polish (PR audited by Codex) |
| #569 | fix(ui): kill /cases mobile overflow | alpha-mobile |
| #570 | fix(ui): universal box-sizing reset | alpha-stability |

### 2.6 Tooling/audit

| PR | Title | Recomendação |
|---|---|---|
| #514 | tools: route audit no-popup mode | **MERGE** (audit infra; companion to my #571) |

---

## 3. Labels a aplicar (sugestão)

Disponíveis no repo: `bug`, `documentation`, `enhancement`, `urgent`, `performance`, `performance-p0`, `performance-p1`, `release-blocker`, `codex`, `feature`, `template-refactor`, `ready-to-merge`, `follow-up`.

### `urgent` + `release-blocker` (alpha-blocker direto)

```
#571 #572 #573 #574 #575    # session 22/05 alpha-readiness
#561                          # calendar N+1 (módulo Agenda)
#521                          # controladoria render budget
#526 #546 #568 #569 #570      # UI mobile críticas (módulos alpha)
#515                          # P0 route fallbacks
```

### `codex`

```
#512 #515 #516 #517 #521 #522 #523 #524 #526 #527 #528 #530 #543 #546 #554 #559 #568 #569 #570
```

### `performance` / `performance-p0` / `performance-p1`

```
performance-p0:  #561 (calendar — alpha módulo)
performance-p1:  #547 #548 #555 #557 #562 #563 #566
performance:     #545 #550 (pool)
```

### `feature`

```
#525 #544 #574 #575    # integrations-gateway, drive, maestro
```

### `documentation`

```
#554 #571
```

### Close candidatos

```
#567       # commit revertido em main 57a109f
```

### `follow-up` (post-alpha)

```
#513       # WhatsApp clone Tier 1+2+3 (BIG novo feature)
#543       # profile dock (DRAFT)
#528       # finance mobile (Finance fora do alpha)
#522       # AILA wiki (AILA off em Lite)
```

---

## 4. Snippet pra aplicar labels (Victor roda)

Single-line script-safe — sem line-break sensitivities:

```bash
cd ~/Projects/casehub

# urgent + release-blocker (alpha-blocker direto)
for n in 571 572 573 574 575 561 521 526 546 568 569 570 515; do gh pr edit $n --add-label "urgent,release-blocker"; done

# codex (frontend assignment)
for n in 512 515 516 517 521 522 523 524 526 527 528 530 543 546 554 559 568 569 570; do gh pr edit $n --add-label "codex"; done

# performance grading
for n in 561; do gh pr edit $n --add-label "performance-p0"; done
for n in 547 548 555 557 562 563 566; do gh pr edit $n --add-label "performance-p1"; done
for n in 545 550; do gh pr edit $n --add-label "performance"; done

# feature
for n in 525 544 574 575; do gh pr edit $n --add-label "feature"; done

# documentation
for n in 554 571; do gh pr edit $n --add-label "documentation"; done

# Close stale-revert
gh pr close 567 --comment "Commit 29dadee was reverted in main via 57a109f after merge. Re-merging would re-introduce the original bug. Closing; rework needed if the perf gain is still wanted."
```

---

## 5. Recomendação de merge wave (D-3 → D-2)

### Wave 1 — alpha-critical do dia 22/05 (Claude session) — 5 PRs

```bash
for n in 571 572 573 574 575; do gh pr merge $n --squash --admin --delete-branch; done
```

### Wave 2 — backend perf alpha módulos — 8 PRs

```bash
# (após rebase ou via --admin)
for n in 561 555 557 562 563 565 566 564; do gh pr merge $n --squash --admin --delete-branch; done
```

### Wave 3 — UI alpha-críticas — 7 PRs (Codex domain mas alpha-blocker)

```bash
for n in 568 569 570 515 521 526 546; do gh pr merge $n --squash --admin --delete-branch; done
```

### Wave 4 — close stale-revert + integrations-gateway

```bash
gh pr close 567 --comment "Reverted in main (57a109f). Rework needed."
gh pr merge 544 --squash --admin --delete-branch
gh pr merge 545 --squash --admin --delete-branch   # predecessor de #550
gh pr merge 550 --squash --admin --delete-branch
gh pr merge 547 --squash --admin --delete-branch
gh pr merge 548 --squash --admin --delete-branch
gh pr merge 549 --squash --admin --delete-branch
gh pr merge 551 --squash --admin --delete-branch
gh pr merge 552 --squash --admin --delete-branch
gh pr merge 525 --squash --admin --delete-branch
```

### Post-alpha (defer)

```
#513 (WhatsApp clone) — BIG novo feature, não alpha
#543 (profile dock) — DRAFT
#528 (finance mobile) — finance fora do alpha
#522 (AILA wiki) — AILA off em Lite
#523 (Notion gate) — feature gate
#524 (admin GDrive) — admin only
#512 (Maestro UI) — Maestro off por padrão
#530 (a11y tap target) — a11y polish
#554 (uiux docs DRAFT)
#559 (kanban polish)
#517 (a11y release notice)
```

---

## 6. Próximos passos

1. **Victor roda** Wave 1-3 (até 18 PRs mergeados). Wave 4 + close opcional.
2. **rsync alpha Mumbai** após Wave 1 (já tem snippet em PR #571).
3. **Re-audit pós-deploy** → `docs/audit/alpha-readiness-2026-05-23.md`.
4. **Smoke E2E** com auth (precisa Playwright + creds).
5. **Latency profile** `/casehub/whatsapp-chat` pós-#545 #547 #548 merge.

---

## 7. Red lines respeitadas

- ✅ Sem auto-merge (snippet pro Victor rodar; classifier bloqueia agent --admin)
- ✅ Toda alegação cita PR # + estado mergeStateStatus real
- ✅ #567 sinalizado pra close (não delete) — preserva auditoria
- ✅ Boundaries: doc-only PR, zero código runtime
