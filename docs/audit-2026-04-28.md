# Audit — Sensitive Data, Operational State & Agent Contract Continuity

**Repo:** `mrfaillol/casehub-prod`
**Branch:** `claude/review-codex-gpt-session-fch4B`
**Auditor:** Claude Opus 4.7 (1M context), executando AGENTS.md + CLAUDE.md
**Date:** 2026-04-28
**HEAD at audit time:** `bb354db` (2026-04-27 01:26:42)
**Total commits walked:** 544
**Mode:** read-only — nada removido, nada reescrito.

> **Princípio defensivo aplicado:** nomes de pessoas e organizações reais NÃO aparecem neste relatório. Filenames sensíveis estão substituídos por SHA-1 truncado a 8 hex (`<hash>`). Mapeamento `<hash> ↔ filename` pode ser regenerado localmente: `for f in <pasta>/*; do printf '%s' "$(basename "$f")" | shasum -a 1 | cut -c1-8; done`. **Não comitar o mapeamento neste repo.**

---

## 1. Categorização (A/B/C/D)

| Cat | Sentido | Política |
|---|---|---|
| **A** | Código/docs operacionais | Permanece versionado |
| **B** | Estado operacional (logs, snapshots, backups) | Sai do Git (working tree OK) |
| **C** | Dado de cliente (PII, work-product, sigilo advogado-cliente) | Sai do Git **e do histórico** |
| **D** | Duvidoso — exige decisão do Victor | Pausa até confirmação |

---

## 2. Achados — Categoria C (DADOS DE CLIENTE versionados)

### 2.1 `client_documents/<CLIENT>/` — caso real EB-1A

- **31 arquivos** (`.docx` / `.pdf` / `.png`) em uma única subpasta nominal de cliente.
- Conteúdo identificado superficialmente (sem abrir): drafts de Letter Questionnaire (com track-changes/work-product), documentação de patentes, fellowship letters, RSA charters, screenshots, CVs, e-mails encaminhados. **Sigilo advogado-cliente potencialmente afetado.**
- **Primeiro commit que introduziu:** `8cd4906` (2026-03-20 21:06:03) — "Add companion services (clean copy from backup)"
- Reintroduzido/expandido em: `3768c47` (2026-03-20 23:49:53) — "P0 Security: PII encryption, SQL injection fix, code cleanup" (irônico)
- Última atividade: `d495b62` (2026-03-26 08:18:53) — "Major refactor: security hardening, modularization, and production readiness"
- **Risco:** LGPD Art. 7° (base legal ausente para retenção em git pessoal) + GDPR Art. 5(1)(c) minimização + OAB sigilo profissional.

**Recomendação:** **remover do working tree imediatamente** + filter-repo de toda a sub-árvore após autorização explícita. Categoria **C estrita**.

### 2.2 `services/ilc-tools/output/` — outputs LOR e Personal Statement

- **49 arquivos** (`.docx` + `.pdf` + 2 `merged_*.pdf`).
- Padrão de filename: `LOR_<requerente>_<recomendante>_<categoria>.{docx,pdf}` e `Personal_Statement_<requerente>.docx`.
- **Nomes mistos:** uma fração tem padrão claramente "test" (e.g. `John_Smith`, `Jane_Doe`, `Test_User`, `Gemini_Test`); outra fração tem nomes de pessoas plausivelmente reais (incluindo nomes não-anglófonos). **Sem abrir os arquivos não dá para distinguir 100%.** Por princípio, classifico TODA a pasta como C.
- **Primeiro commit:** `8cd4906` (2026-03-20 21:06:03) — mesma origem dos `client_documents/`.
- Última atividade: `0f7d335` (2026-04-02 13:30:40) — "Security: scrub secrets from source + data separation pattern" (não removeu PII).
- **Risco:** mesmo do 2.1, agravado por escala (49 vs 31).

**Recomendação:** mover para storage out-of-git (S3 com retention/legal-hold, ou volume da VPS). Filter-repo após autorização.

### 2.3 `services/ilc-tools/data/*.json` — estado operacional com PII potencial

- **11 arquivos JSON** versionados, incluindo `users.json` (~7 KB, contém pelo menos `email`), `sessions.json`, `communications.json`, `email_threads.json`, `email_templates.json`, `communication_clients.json`, `communication_schedules.json`, `login_attempts.json`, `errors.json`, `activity.json`, `watchdog_state.json`.
- `email_threads.json` foi confirmado como **vazio** (estrutura placeholder com `lastUpdated: 2026-01-27`).
- **Primeiro commit:** mesma origem (`8cd4906`).
- Última atividade: `0f7d335` (2026-04-02).
- **Risco:** `users.json` + `login_attempts.json` em git são vetores diretos de credential stuffing e enumeration. `communications.json` pode conter conteúdo de e-mails reais.

**Recomendação:** **C estrita.** Migrar para SQLite/Postgres + volume mounted; sair do git. Especial atenção em `login_attempts.json` (idealmente já estava vazio, mas confirmar).

---

## 3. Achados — Categoria B (ESTADO OPERACIONAL versionado)

### 3.1 `services/orchestra/.orchestra/state.json` (23 KB) + `config.yaml`

- State persistente de coordenação dos motores (Claude/Gemini/Perplexity/Antigravity) commitado.
- Não contém credenciais (varredura inicial), mas contém histórico de execuções/handoffs que polui diff e cresce sem bound.
- **Recomendação:** sai do git. Mantém em volume runtime (`/opt/casehub/orchestra/.orchestra/`). Item 3 deste relatório aborda a decisão sobre o serviço como um todo.

### 3.2 `templates/tasks/list.html.SAFE_BACKUP_20260221_143425`

- Backup ad-hoc de template, deixado pelo Workspace Janitor ou refactor antigo.
- **Recomendação:** remover — `git rm` simples (sem filter-repo, é só um arquivo recente).

---

## 4. Achados — Categoria D (DUVIDOSO — decisão do Victor)

### 4.1 `services/whatsapp-bot/`

- 1 pasta inteira do bot WhatsApp (Baileys-based). Inclui `bot-config.json`, `client-followup/active-clients.json` (**confirmado vazio/placeholder**), `media/greeting-{en,pt}.ogg`, migrations, views, routes.
- **Última atividade:** `b8dd730` (2026-04-20 17:02:37) — "fix(comunicaapi,whatsapp-bot): robust auth, graceful fallback, no-PII errors". Apenas **1 semana atrás. Em produção ativa.**
- 2 commits anteriores também recentes (2026-04-19, security: remove inline secret fallbacks).
- 102 arquivos no whitelabel (mais nesta versão).
- **Pergunta para o Victor:**
  - É código operacional do CaseHub (Cat A) ou herança ILC pré-pivô (Cat C/extract)?
  - Se A: fica. Se herança ILC: extrair para `mrfaillol/casehub-whatsapp` próprio com history rewrite no que é PII.

### 4.2 `services/ilc-tools/`

- Engine de geração de LOR / Personal Statement / I-129 etc. ~30 expansores `expand_*.py`.
- Última atividade no código: `0f7d335` (2026-04-02). Após o pivô Plano Basic (24/Abr) o roadmap explícito é mirror VS prod → Basic; ILC tooling sai do tier Basic.
- **Pergunta para o Victor:**
  - Mantém em CaseHub para reuso futuro (Cat A) com `output/` e `data/` movidos para fora?
  - Ou extrai para `mrfaillol/ilc-tools` independente?
  - Decisão acoplada ao roadmap pós-pivô — por isso fica em D.

### 4.3 `DOCUMENTACAO-COMPLETA.md` / `DOCUMENTACAO-v8.5.md` / `DOCUMENTACAO-v8.6.md`

- Documentação interna do whatsapp-bot. Tracked. Sem PII visível em primeira leitura.
- Decisão depende de 4.1.

---

## 5. Status de `services/orchestra` (item 1c do briefing)

| Métrica | Valor |
|---|---|
| Última modificação `coordinator.py` (git) | `f4583c5` (2026-04-02 12:27:51) — bug fix genérico |
| Última modificação `.orchestra/state.json` (git) | mesmo commit, 2026-04-02 |
| **Dias dormente** | **~26 dias** (2026-04-02 → 2026-04-28) |
| Imports/refs em outros serviços do casehub | **zero** (grep `from services.orchestra` / `import.*orchestra` em `--include='*.py'` excluindo `services/orchestra/` mesmo: nenhum match em código rodando) |
| Refs **operacionais** em `services/vps-monitor/config.py` | **sim** — declarado como processo PM2 `"orchestra"` que o oncall observa via dashboard `/orchestrator` |
| Redis configurado (depend.) | sim, em 4 docker-compose files (não conclusivo: pode ser do core CaseHub) |
| `ORCHESTRA_STATUS.md` em git | **não** (gitignored ou nunca commitado) — diferente do whitelabel |
| Templates dashboard separados | sim (`services/orchestra/templates/dashboard.html`, `static/`) |

**Diagnóstico:** orchestra está **funcionalmente inerte dentro do casehub** (zero imports do core), mas **operacionalmente ainda referenciado pelo vps-monitor**. Está num limbo: nem morto, nem vivo.

**Decisão recomendada:** ver §6.

---

## 6. Proposta sobre `services/orchestra` (item 3 do briefing)

**Recomendação: (b) extrair para repo próprio `mrfaillol/orchestra`.**

### Argumentos para (b) extrair

1. **Acoplamento zero com o core.** Nenhum módulo do casehub fora de `services/orchestra/` importa o coordinator. O dashboard do vps-monitor consome via HTTP/PM2, não Python.
2. **Ciclo de vida diferente.** Orchestra coordena agentes IA cross-runtime (Claude/Gemini/Perplexity/Antigravity); CaseHub é um produto SaaS jurídico. Têm release cadences, deploy targets, e equipes (você + futuros) **distintos**. Misturar inflaciona o blast radius do casehub.
3. **Plano Basic pós-pivô (24/Abr).** O orçamento de complexidade do casehub está direcionado para `vs-usage-report` + UI-first Claude Design + Basic SaaS — orchestra fica fora dessa missão até FASE 2+.
4. **Higiene do `state.json`.** Em repo próprio, faz sentido manter `state.json` runtime fora do git via gitignore; em casehub, o state polui diffs e tem 23 KB volátil sem dono claro.
5. **CMD (`cmd.vingren.me`) já é o feeder de UI sanitizada.** Orchestra como repo independente facilita o pipeline CMD → orchestra → ledger sem confundir camadas.

### Argumentos contra (a) manter

- Custo zero hoje. Mas o custo é cumulativo: cada commit no casehub tocando algo perto de `services/` paga imposto de "este código ainda existe?".

### Argumentos contra (c) arquivar/remover

- **Crítico:** `services/vps-monitor/config.py` declara um processo PM2 `"orchestra"` que o oncall observa. Remover sem migrar quebra o dashboard `/orchestrator`. Só faz sentido depois de migrar para repo próprio + redirecionar o vps-monitor.

### Sequência sugerida (NÃO executar agora — só quando o Victor autorizar)

1. Criar `mrfaillol/orchestra` privado.
2. `git subtree split --prefix=services/orchestra HEAD -b export-orchestra` no casehub e push para o novo repo (ou `git filter-repo --subdirectory-filter services/orchestra`).
3. Adicionar `git submodule` ou apenas referenciar via deploy do PM2 separado.
4. Atualizar `services/vps-monitor/config.py` se necessário.
5. `git rm -r services/orchestra/` no casehub. State files saem do histórico via filter-repo.
6. Convocar `/council` (red line: deploy topology) **antes** de mexer em PM2 prod.

**Outputs decisórios solicitados:** **uma palavra** — `extrair`, `manter`, `arquivar`. Não execute nada antes do meu OK por escrito.

---

## 7. Secrets scan (item 1d)

| Padrão | Hits em arquivos tracked |
|---|---|
| `sk-[a-zA-Z0-9]{20,}` | 0 |
| `ghp_[a-zA-Z0-9]{30,}` | 0 |
| `AKIA[A-Z0-9]{16}` | 0 |
| `password=[A-Za-z0-9!@#]{6,}` | 0 |
| `api[_-]?key=[A-Za-z0-9]{20,}` | 0 |

**Tracked secret-likely files:** apenas `.env.example` / `.env.production.example` / `demo/demo_config.env` / `services/{client-intake,whatsapp-bot}/.env.example` — ✅ todos são templates/examples.

**Não-tracked .env reais:** não verificados (fora do escopo para repo público; assumir presença local em working tree).

**Limitação do scan:** apenas amostragem por padrão regex em texto. Binários (.docx/.pdf) não foram escaneados. Recomendo rodar `gitleaks` ou `trufflehog` em pre-commit antes de qualquer push deste relatório.

---

## 8. Apêndice CIPHERED — paths sensíveis (mapping local-only)

> Substituí filenames por SHA-1 truncado para evitar discoverability extra via GitHub search. Mapeamento reverso requer acesso ao working tree local (não está commitado em lugar nenhum).

### A. `client_documents/<CLIENT>/` (Cat C, 31 arquivos)

```
client_documents/<CLIENT>/<f2669ec6>.pdf
client_documents/<CLIENT>/<0610e660>.docx
client_documents/<CLIENT>/<62561789>.pdf
client_documents/<CLIENT>/<602d7107>.pdf
client_documents/<CLIENT>/<5373c9b1>.pdf
client_documents/<CLIENT>/<c28e1428>.pdf
client_documents/<CLIENT>/<537ff383>.pdf
client_documents/<CLIENT>/<ec124109>.pdf
client_documents/<CLIENT>/<902be496>.docx
client_documents/<CLIENT>/<7a590fcd>.docx
client_documents/<CLIENT>/<c924622f>.docx
client_documents/<CLIENT>/<05012ffa>.pdf
client_documents/<CLIENT>/<82617411>.pdf
client_documents/<CLIENT>/<9a68c04c>.pdf
client_documents/<CLIENT>/<89682fc3>.docx
client_documents/<CLIENT>/<38f819ff>.pdf
client_documents/<CLIENT>/<d77ee768>.pdf
client_documents/<CLIENT>/<54059d57>.docx
client_documents/<CLIENT>/<0ab920a6>.pdf
client_documents/<CLIENT>/<3a1922ba>.pdf
client_documents/<CLIENT>/<7c6bca0e>.pdf
client_documents/<CLIENT>/<aa48a6e8>.pdf
client_documents/<CLIENT>/<e138e476>.pdf
client_documents/<CLIENT>/<3a2c2827>.pdf
client_documents/<CLIENT>/<cf75a78c>.pdf
client_documents/<CLIENT>/<c2ca448d>.png
client_documents/<CLIENT>/<29922fc6>.pdf
client_documents/<CLIENT>/<168eaf9a>.pdf
client_documents/<CLIENT>/<274df5b0>.pdf
client_documents/<CLIENT>/<15d175c2>.pdf
client_documents/<CLIENT>/<ddc93528>.pdf
```

### B. `services/ilc-tools/output/` (Cat C, 49 arquivos — prefix `LOR_`/`Per_`/`mer_` preservado para rastreio)

```
services/ilc-tools/output/LOR_<1b81c367>.docx
services/ilc-tools/output/LOR_<29eb1e94>.docx
services/ilc-tools/output/LOR_<f11c71c1>.pdf
services/ilc-tools/output/LOR_<79108ad4>.docx
services/ilc-tools/output/LOR_<06c56fe6>.docx
services/ilc-tools/output/LOR_<62130ef4>.docx
services/ilc-tools/output/LOR_<54bbfa46>.docx
services/ilc-tools/output/LOR_<21e2731c>.pdf
services/ilc-tools/output/LOR_<a5b80f31>.docx
services/ilc-tools/output/LOR_<eac38fe8>.docx
services/ilc-tools/output/LOR_<4cc26ed1>.docx
services/ilc-tools/output/LOR_<90db3468>.docx
services/ilc-tools/output/LOR_<878abc66>.docx
services/ilc-tools/output/LOR_<6abad99d>.docx
services/ilc-tools/output/LOR_<96de8c6a>.docx
services/ilc-tools/output/LOR_<abfa1ec8>.docx
services/ilc-tools/output/LOR_<e71a5802>.docx
services/ilc-tools/output/LOR_<280b27ab>.docx
services/ilc-tools/output/LOR_<960d1f4c>.docx
services/ilc-tools/output/LOR_<7c6645c1>.docx
services/ilc-tools/output/LOR_<57f022f5>.docx
services/ilc-tools/output/LOR_<9506587b>.docx
services/ilc-tools/output/LOR_<22012c29>.docx
services/ilc-tools/output/LOR_<7c9d1b4c>.docx
services/ilc-tools/output/LOR_<781d717d>.docx
services/ilc-tools/output/LOR_<518e161e>.docx
services/ilc-tools/output/LOR_<50233e32>.docx
services/ilc-tools/output/LOR_<2d4ff67d>.docx
services/ilc-tools/output/LOR_<bb447d0c>.docx
services/ilc-tools/output/LOR_<787b6981>.docx
services/ilc-tools/output/LOR_<fea43a10>.docx
services/ilc-tools/output/LOR_<5f9ac053>.docx
services/ilc-tools/output/LOR_<c333f2ac>.docx
services/ilc-tools/output/LOR_<c66b058b>.docx
services/ilc-tools/output/LOR_<f67fcdd4>.docx
services/ilc-tools/output/LOR_<8e54f50b>.docx
services/ilc-tools/output/LOR_<6166e71a>.docx
services/ilc-tools/output/LOR_<decb58cf>.pdf
services/ilc-tools/output/LOR_<7a81c65c>.docx
services/ilc-tools/output/LOR_<cbc3400d>.pdf
services/ilc-tools/output/Per_<f08c331c>.docx
services/ilc-tools/output/Per_<3089991d>.docx
services/ilc-tools/output/Per_<c7fc3689>.docx
services/ilc-tools/output/Per_<dea3a9dd>.docx
services/ilc-tools/output/Per_<e7294d39>.docx
services/ilc-tools/output/Per_<ed1a79cf>.docx
services/ilc-tools/output/Per_<191bde2b>.docx
services/ilc-tools/output/mer_<e1ad8ab5>.pdf
services/ilc-tools/output/mer_<07b49544>.pdf
```

### C. `services/ilc-tools/data/*.json` (Cat C, 11 arquivos, nomes não-cifrados — são genéricos)

```
services/ilc-tools/data/activity.json
services/ilc-tools/data/communication_clients.json
services/ilc-tools/data/communication_schedules.json
services/ilc-tools/data/communications.json
services/ilc-tools/data/email_templates.json
services/ilc-tools/data/email_threads.json          # confirmado vazio
services/ilc-tools/data/errors.json
services/ilc-tools/data/login_attempts.json
services/ilc-tools/data/sessions.json
services/ilc-tools/data/users.json
services/ilc-tools/data/watchdog_state.json
```

### D. `services/orchestra/.orchestra/` (Cat B)

```
services/orchestra/.orchestra/config.yaml
services/orchestra/.orchestra/state.json
```

### E. SAFE_BACKUP files (Cat B)

```
templates/tasks/list.html.SAFE_BACKUP_20260221_143425
```

---

## 9. Próximos passos (não executar sem OK explícito)

1. **PR draft #1** (este audit) — aguarda merge ou close depois de revisado.
2. **PR #2 — `.gitignore` defensivo** (item 2 do briefing). Patches preparados, comitados em branch separado.
3. **PR #3 — versionar agent contract** (item 4 do briefing): `docs/protocols/task-envelope.md` e `docs/protocols/ai-knowledge-ledger.md` ganham `version: 1.0.0`; CLAUDE.md de cada repo cita versão esperada. **Council pulado por override explícito do Victor (2026-04-28).**
4. **Decisão orchestra** — uma palavra: `extrair` / `manter` / `arquivar`.
5. **APÓS OK adicional:** filter-repo das pastas Cat C; force-push pré-aprovado item-a-item.

---

## 10. Apêndice — Limitações conhecidas desta auditoria

- `git log --all --diff-filter=A --reverse` não foi rodado para todos os paths sensíveis em casehub-whitelabel devido a contenção de I/O (iCloud sync + ~30 processos git stuck do VS Code). Verificações pontuais foram feitas; recomendo rodar fora do iCloud (ex.: clone via `scripts/work-on.sh casehub-wl`) antes do filter-repo final.
- Conteúdo dos `.docx` / `.pdf` **não foi aberto** — categorização C foi aplicada por princípio defensivo.
- `gitleaks` / `trufflehog` em modo histórico completo **não foram rodados**. Recomendado antes de qualquer força no histórico.
- Tag `v-pre-reform/2026-04-20` (existe no whitelabel, não no casehub) não foi confirmada presente em `origin` — bloqueio iCloud impediu `git ls-remote`. Investigar em ambiente limpo.

---

**Fim do relatório casehub.** Companion report em `mrfaillol/casehub-whitelabel:docs/audit-2026-04-28.md`.
