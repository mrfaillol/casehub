# CaseHub SHA blocklist — Rollback auto aborta se $PREV_SHA casar com linha aqui.
#
# Ruling: 2026-04-24-casehub-gitops-deploy (condition S7).
#
# Formato: uma linha por SHA curto (7+ chars). Linhas # são comentários.
# Adicionar via PR (nunca edit direto em main).
#
# Processo para adicionar:
#   1. Finding sentinela identifica CVE em commit já mergeado.
#   2. PR contendo ÚNICA mudança: append SHA aqui.
#   3. Merge via branch protection normal.
#   4. Deploys futuros que tentarem rollback para este SHA → abort + page on-call.
#
# (vazio inicialmente — populado conforme findings)
