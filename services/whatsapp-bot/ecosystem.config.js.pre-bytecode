module.exports = {
  apps: [{
    name: "whatsapp-bot",
    script: "server.js",
    cwd: "/var/www/immigrant.law/whatsapp-bot",
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: "500M",
    env: {
      NODE_ENV: "production",
      PORT: 3001
    },
    error_file: "/var/log/pm2/whatsapp-bot-error.log",
    out_file: "/var/log/pm2/whatsapp-bot-out.log",
    log_date_format: "YYYY-MM-DD HH:mm:ss Z"
  }]
};
